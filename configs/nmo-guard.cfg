"nmoguard"
{
	// Objectives listed here won't trigger an objective chain skip if they're 
	// completed ahead of time. Some maps erroneously do this via entity I/O
	"skip_blacklist"
	{
		// "map name" "objective name"
		
		// nmo_experiment tries to complete "Gather supplies before leaving" when the round starts
		"nmo_experiment_v3"			"12"
		"nmo_experiment_v3_fix"		"12"
	}
}